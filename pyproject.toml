[project]
name = "claude-token-budget"
version = "0.1.0"
description = "Claude.ai token budget scraper"
authors = [
    {name = "no"}
]
readme = "START_HERE.md"
package-mode = false
requires-python = ">=3.12"
dependencies = [
    "playwright (>=1.58.0,<2.0.0)",
    "browser-cookie3 (>=0.20.1,<0.21.0)",
    "httpx (>=0.28.1,<0.29.0)",
    "curl-cffi (>=0.14.0,<0.15.0)"
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
